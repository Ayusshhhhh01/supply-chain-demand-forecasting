# Data

Raw sales data is not committed yet. I am starting with a public demand-forecasting dataset and will keep the downloaded file outside the repository.

Initial checks: date coverage, missing values, duplicate rows, zero-sales periods, SKU coverage and location-level demand patterns.
